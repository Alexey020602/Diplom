@attribute [Authorize(Roles = "Cip")]
@using Client.Shared.Select
@using Microsoft.AspNetCore.Authorization
@using Model.Partners
@inherits Client.Shared.List.SearchableStyledList<Model.Partners.PartnerShort>

@rendermode RenderMode.InteractiveWebAssembly
@page "/"
@page "/partners"

<PageTitle>Патнеры университета</PageTitle>
@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override RenderFragment Filters =>
        @* @<UniversalCustomSelect TItem=@PartnerType @bind-Value:get=@PartnerType @bind-Value:set=@SelectPartnerType/>; *@
        @<RadzenDataFilter Data=@items Auto=false ApplyFilterText="Применить" ClearFilterText="Очистить">
            <Properties>
                <RadzenDataFilterProperty Property=@(nameof(PartnerShort.Name)) Title="Имя" />
                <RadzenDataFilterProperty Property="@nameof(Partner.Type)"/>
            </Properties>
        </RadzenDataFilter>;
    
    }