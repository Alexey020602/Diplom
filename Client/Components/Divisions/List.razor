@using Client.Shared.List;
@using SharedModel
@using Client.Shared.Select;
@using DataBase.Models;

@inherits SearchableStyledList<DivisionShort>

@page "/divisions"

<PageTitle>Подразделения</PageTitle>
@{
    base.BuildRenderTree(__builder);
}

@* <SearchableStyledList ListDelegate=@this /> *@
@code {
    protected override RenderFragment Filters => @<UniversalCustomSelect 
    TItem=Faculty 
    @bind-SelectedItem:get=@faculty 
    @bind-SelectedItem:set=@SelectFaculty
    GetStringIdentifier=@(faculty => faculty.Id.ToString())/>;
}