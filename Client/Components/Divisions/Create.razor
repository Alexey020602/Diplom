@using DataBase.Models
@using Services

@inject IDivisionsService api
@page "/divisions/create"

<Form Division=@division OnSubmit=@OnValidSubmit/>

@code {
    private Division division = new();
    private Task OnValidSubmit() => api.Create(division);
}
