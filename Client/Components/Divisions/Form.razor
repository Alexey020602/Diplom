@attribute [Authorize(Roles = "Ctt")]
@using Client.Services.Api
@using Client.Services.Api.BaseApi
@using Client.Shared.Select
@using Client.Shared.Text
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Forms
@using Model
@using Model.Divisions
@inherits Client.Shared.Form.BaseForm<Model.Divisions.Division>
@inject IReadApi<Faculty> FacultiesApi

@{
    base.BuildRenderTree(__builder);
}

@code {

    protected override RenderFragment Content =>
        @<RadzenStack>
            @ShortName
            @FullName
            @Site
            @Contacts
            @Faculty
            @Directions
    </RadzenStack>;

    private RenderFragment ShortName => FormRow(
        "Краткое название",
        "ShortName",
        @<RadzenTextBox Style="width: 100%" Name="ShortName" @bind-Value=@Item.ShortName />
        );

    private RenderFragment FullName => FormRow(
        "Полное название",
        "FullName",
        @<RadzenTextBox Style="width: 100%" Name="FullName" @bind-Value=@Item.FullName/>
    );

    private RenderFragment Site => FormRow(
        "Сайт",
        "Site",
        @<RadzenTextBox Style="width: 100%" Name="Site" @bind-Value=@Item.Site/>
    );

    private RenderFragment Contacts => FormRow(
        "Контактные данные",
        "Contacts",
        @<RadzenTextArea Style="width: 100%" Name="Contacts" @bind-Value=@Item.Contacts/>
    );

    private RenderFragment Faculty => FormRow(
        "Факультет",
        "Faculty",
        @<UniversalCustomSelect 
             Style="width: 100%" TItem=@Faculty Name="Faculty" LoadItems="() => FacultiesApi.ReadAll()" @bind-Value=@Item.Faculty/>
    );

    private RenderFragment Directions => FormRow(
        "Направление",
        "Directions",
        @<UniversalMultipleSelect  
             Style="width: 100%" Name="Directions" TApi=@IDirectionsService TItem=@Direction @bind-Value=@Item.Directions/>
    );

}