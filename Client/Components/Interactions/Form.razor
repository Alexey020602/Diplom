@using Client.Services.Api
@using Client.Services.Api.BaseApi
@using Client.Shared.Select
@using Client.Shared.Text
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Forms
@using Model
@using Model.Divisions
@using Model.Interactions
@using PartnerShort = Model.Partners.PartnerShort
@attribute [Authorize(Roles = "Ctt")]
@inherits Client.Shared.Form.BaseForm<Model.Interactions.Interaction>

@inject IPartnersService PartnersService
@inject  IDirectionsService DirectionsService
@inject IDivisionsService DivisionsService

@{
    base.BuildRenderTree(__builder);
}

@code {

    protected override RenderFragment Content =>
        @<RadzenStack>
            @Directions
            @ContactCode
            @Theme
            @Partner
            @Division
            @Type
            @Begin
            @End
            @SigningDate
    </RadzenStack>;

    private RenderFragment Directions => FormRow(
        "Направления",
        "Directions",
        @<UniversalMultipleSelect 
             Style="width: 100%" TApi=@IDirectionsService TItem=@Direction @bind-Value=@Item.Directions Name="Directions"/>
        );

    private RenderFragment ContactCode => FormRow(
        "Код",
        "Code",
        @<RadzenTextBox
             Style="width: 100%" @bind-Value=@Item.ContactCode Name="Code"/>
        );

    private RenderFragment Theme => FormRow(
        "Тема",
        "Theme",
        @<RadzenTextArea
             Style="width: 100%" @bind-Value=@Item.Theme Name="Theme"/>
    );

    private RenderFragment Partner => FormRow(
        "Партнер",
        "Partner",
        @<UniversalCustomSelect 
             TItem="@PartnerShort"
             Style="width: 100%"
             Name="Partner" 
             @bind-Value=@Item.Partner 
             LoadItems="@( async () => (await PartnersService.ReadAll(new PartnersFilter())).Data.ToList())"/>
    );

    private RenderFragment Division => FormRow(
        "Подразделение",
        "Division",
        @<UniversalCustomSelect 
             TItem="@DivisionShort"
             Style="width: 100%"
             Name="Division" 
             @bind-Value=@Item.Division
             LoadItems="@(async () => (await DivisionsService.ReadAll(new DivisionsFilter())).Data.ToList())"
             />
    );

    private RenderFragment Type => FormRow(
        "Тип",
        "Type",
        @<UniversalSelect 
             TItem="@InteractionType"
             Style="width: 100%"
             @bind-Value=@Item.Type
             Name="Type"
             TApi="IReadApi<InteractionType>"
             />
    );

    private RenderFragment Begin => FormRow(
        "Начало",
        "Begin",
        @<RadzenDatePicker
             Style="width: 100%" TValue=@DateOnly Name="Begin" Value=@Item.Begin Change=@(begin => Item.Begin = DateOnly.FromDateTime(begin ?? DateTime.Today))/>
        );
    private RenderFragment End => FormRow(
        "Конец",
        "End",
        @<RadzenDatePicker
             Style="width: 100%" TValue=@DateOnly Name="End" Value=@Item.End Change=@(end => Item.End = DateOnly.FromDateTime(end ?? DateTime.Today))/>
    );
    private RenderFragment SigningDate => FormRow(
        "Начало",
        "SigningDate",
        @<RadzenDatePicker
             Style="width: 100%" TValue=@DateOnly Name="SigningDate" Value=@Item.SigningDate Change=@(signingDate => Item.SigningDate = DateOnly.FromDateTime(signingDate ?? DateTime.Today))/>
    );
}